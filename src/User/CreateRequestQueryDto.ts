import { IsEnum, IsNotEmpty, IsUrl, IsUUID } from 'class-validator';
import { GrantType } from '../../enumerations/GrantType';

export class CreateRequestQueryDto {

    constructor(params: CreateRequestQueryDto) {
    }
}